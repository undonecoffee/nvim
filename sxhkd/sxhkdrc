super + c
    dmenu_run
super + u
    spectacle
super + at
    kitty

super + alt + q 
    bspc quit
super + alt + x 
    reboot
super + alt + r
    bspc wm -r
super + z
    bash $DOTFILES/sxhkd/startsxhkd.sh
super + w
    bspc node -c
super + apostrophe
    pactl set-sink-volume @DEFAULT_SINK@ +5%
super + slash
    pactl set-sink-volume @DEFAULT_SINK@ -5%

super + q
    bspc desktop -f "q-main"
super + e
    bspc desktop -f "e-obs"
super + r
    bspc desktop -f "r-misc"
super + t
    bspc desktop -f "t-term"
super + y
    bspc desktop -f "y-youtube"
super + o
    bspc desktop -f "o-obsidian"
super + p
    bspc desktop -f "p-system"
super + a
    bspc desktop -f "a-ai"
super + d
    bspc desktop -f "d-discord"
super + f
    bspc desktop -f "f-krita"
super + g
    bspc desktop -f "g-github"
super + b
    bspc desktop -f "b-browser"
super + n
    bspc desktop -f "n-school"
super + m
    bspc desktop -f "m-music"
super + space
    bspc desktop -f "space-code"

super + shift + q
    bspc node -d "q-main"
super + shift + e
    bspc node -d "e-obs"
super + shift + r
    bspc node -d "r-misc"
super + shift + t
    bspc node -d "t-term"
super + shift + y
    bspc node -d "y-youtube"
super + shift + o
    bspc node -d "o-obsidian"
super + shift + p
    bspc node -d "p-system"
super + shift + a
    bspc node -d "a-ai"
super + shift + d
    bspc node -d "d-discord"
super + shift + f
    bspc node -d "f-krita"
super + shift + g
    bspc node -d "g-github"
super + shift + b
    bspc node -d "b-browser"
super + shift + n
    bspc node -d "n-school"
super + shift + m
    bspc node -d "m-music"
super + shift + space
    bspc node -d "space-code"

  



super + h
    bspc node -f west
super + j
    bspc node -f south
super + k
    bspc node -f north
super + l
    bspc node -f east

super + shift + h
    bspc node -s west
super + shift + j
    bspc node -s south
super + shift + k
    bspc node -s north
super + shift + l 
    bspc node -s east

super + alt + h
    bspc node -z left 20 0
super + alt + l
    bspc node -z right 20 0
super + alt + k
    bspc node -z top 0 20
super + alt + j
    bspc node -z bottom 0 20

super + ctrl + l
    bspc node -z left -20 0
super + ctrl + h
    bspc node -z right -20 0
super + ctrl + j
    bspc node -z top 0 -20
super + ctrl + k
    bspc node -z bottom 0 -20
